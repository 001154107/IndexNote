# =============================================================================
# IndexNote — Dependencies
# Install: pip install -r requirements.txt
# For audio/video support: pip install -r requirements.txt[media]
# =============================================================================

# --- Core Orchestration (LlamaIndex) -----------------------------------------
llama-index-core>=0.12.0
llama-index-readers-docling>=0.3.0
llama-index-node-parser-docling>=0.3.0

# --- Graph Store (Kuzu) ------------------------------------------------------
kuzu>=0.8.0
llama-index-graph-stores-kuzu>=0.4.0

# --- Vector Store (ChromaDB) -------------------------------------------------
chromadb>=0.6.0
llama-index-vector-stores-chroma>=0.4.0

# --- LLM Providers -----------------------------------------------------------
# Ollama (local, default)
llama-index-llms-ollama>=0.5.0
llama-index-embeddings-ollama>=0.5.0

# Gemini (cloud, optional — uncomment if using Gemini)
llama-index-llms-google-genai>=0.4.0
llama-index-embeddings-google-genai>=0.4.0

# --- Document Parsing --------------------------------------------------------
docling>=2.30.0

# --- File System Watching ----------------------------------------------------
watchdog>=6.0.0

# --- Environment & Config ----------------------------------------------------
python-dotenv>=1.0.0

# --- Audio Transcription (optional — install separately if needed) -----------
faster-whisper>=1.1.0

# --- Video Processing (optional — install separately if needed) --------------
opencv-python-headless>=4.10.0
scenedetect[opencv]>=0.6.0

# --- Utilities ----------------------------------------------------------------
rich>=13.0.0
